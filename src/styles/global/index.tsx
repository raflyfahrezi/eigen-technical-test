import React from 'react'
import { Global as GlobalStyles } from '@emotion/react'

import { sGlobalStyles } from './styles'

const Global = () => {
  return <GlobalStyles styles={sGlobalStyles} />
}

export default Global
